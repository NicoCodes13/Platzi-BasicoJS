var nombre = 'sacha', edad = 28

function imprimirEdad(nombre, edad) {
    console.log(`${nombre} tiene ${edad} años`)
}

imprimirEdad('Nicolas',23)
// NOTE palabra this indica que se usa la variable de la clase y no la de la funcion